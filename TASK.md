# Main Project Tasks

## Objective
Keep the Viper project aligned with the architecture described in the master `PLAN.md` and track the highest-level work items.

## Task List
- [x] **Core Architecture:** Implement the Orchestrator, Model Context Protocol (MCP) tool server, and local HTTP A2A server and wire them into the VS Code extension.
- [x] **Specialist Agents:** Implement and host the main specialist agents (analysis, refactoring, documentation, security, watcher, context archive, etc.).
- [ ] **Intelligent Context Management & Long-Term Memory:** Finish the pruning/summarization pipeline and the full `ContextArchiveAgent` workflows described in `PLAN.md`.
- [ ] **UI & UX:** Polish the React-based webview UI (streaming responses, clear status updates, richer controls).
- [ ] **Testing:** Add comprehensive unit and integration tests for services, tools, the Orchestrator, and the UI.
- [ ] **Documentation:** Keep `PLAN.md`, `PROGRESS.md`, `TASK.md`, `docs/*.md`, and `_folder_overview*.md` files in sync with the evolving codebase.